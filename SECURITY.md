# 安全说明

## 凭证原则

云服务凭证必须只存在于服务端环境变量，不进入浏览器、不写入源码、不提交 Git。

发现疑似泄露时：

1. 立即在阿里云或对应平台禁用并轮换凭证。
2. 检查 Git 历史、构建产物、日志、截图和外发文件。
3. 更新部署平台 Secret，不在 Issue 或提交说明中粘贴密钥。
4. 运行 `npm run security:check` 和完整构建验证。

## 公开仓库范围

录音、ASR 原文、数据库、客户材料、导出结果和本地 `.env` 均不属于公开仓库内容。
